[project]
name = "time-evolving-crypto"
version = "0.3.0"
description = "Proof of concept for Time-Evolving Ephemeral Decryption Protocol"
dependencies = [
    "cryptography",
    "flask",
    "requests",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
